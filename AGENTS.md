# Repository guidance

## Commit messages

Use Conventional Commits. For changes within one top-level project, use its
folder name as the scope, such as `feat(llm): ...`, `fix(harness): ...`, or
`chore(codex): ...`. For changes spanning multiple projects, use `repo` as the
scope.
