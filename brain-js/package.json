{
  "name": "brain-js-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for the brain-js workflow engine and runtime adapters",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "workspaces": [
    "workspaces/brain",
    "workspaces/codex"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck:examples": "tsc -p workspaces/codex/tsconfig.examples.json",
    "typecheck:tests": "tsc -p tsconfig.tests.json",
    "test": "npm run build && npm run typecheck:examples && npm run typecheck:tests && node --test workspaces/brain/tests/*.test.ts workspaces/codex/tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.18"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
